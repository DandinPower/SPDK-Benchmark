
#include <utils.h>

// void* mem_allocation(size_t size) {

//     return malloc(size);

//     // return spdk_zmalloc(size, ALIGN_SIZE, NULL, SPDK_ENV_SOCKET_ID_ANY, SPDK_MALLOC_DMA);

// #ifdef MEM_ALLOCATION_SPDK
    
// #else
//     return malloc(size);
// #endif

// }